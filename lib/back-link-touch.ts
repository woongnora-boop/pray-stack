/**
 * 돌아가기·목록으로 등 텍스트 링크: 모바일에서만 터치 영역 확대(시각적 크기는 과하지 않게).
 * `className`에 기존 스타일과 함께 붙여 씁니다.
 */
export const backLinkTouchClassName =
  'max-md:inline-flex max-md:min-h-11 max-md:items-center max-md:py-2.5 max-md:px-1 max-md:-mx-1';
